# Real Provider Requirements

To replace placeholder provider data, each provider must deliver:

- GPU type
- hourly price
- availability / supply indicator
- region
- timestamp
- provider name

First target:

RunPod

Required:

- RUNPOD_API_KEY
- official API endpoint
- response parser
- fallback behavior
